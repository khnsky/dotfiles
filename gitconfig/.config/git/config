[user]
	email = khnsky@khnsky.cc
	name  = Piotrek Kochański

[help]
	# negative - execute autocorected command immediately, 0 - only show,
	# positive execute autocorected command after n tenth of a second
	autocorrect = -1

[color]
	ui = auto

[alias]
    sb = status -sb

[commit]
    # not sure if this is right but:
    #   1 - show diff in commit message editor
    #   2 - also show diff between unstaged changes?
    verbose = 2
